npm install react-scripts 
npm install react-router-dom
npm install axios

other issues:
npm cache clean --force
Remove-Item -Path node_modules -Recurse -Force
npm install
npm install webpack
npm audit fix --force

pip installing flask, CORS, etc. may be necessary to run the backend if not already installed